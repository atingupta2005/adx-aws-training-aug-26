# =============================================================================
# Git ignore — student GitHub repo (adx-aws-training-aug-26)
# =============================================================================
# BEFORE EVERY PUSH: bash scripts/verify_student_push.sh
#
# Safe to commit (student package):
#   README.md, 00_*.md, Module_01–05/,
#   assets/iam/, assets/module_01–05/, assets/logstash_in_depth/,
#   assets/common/, assets/ingest_s3_to_adx.sh, scripts/verify_student_push.sh
#
# Keep local only until issued: Logstash_In_Depth/, Module_06–08, Instructor_Materials/,
# _private_validation/, *_Instructor.md, assets/REAL_VS_LAB_DATA.md (trainer)
# Module_04 + assets/module_04 are issued to the student repo.
# Module_05 + assets/module_05 issued for Logstash week.
# =============================================================================

# --- Later modules / trainer-only — local copy only; do not commit ---
# Each blocked folder may have its own .gitignore (* + !.gitignore).
Logstash_In_Depth/
Module_08_AWS_GuardDuty/
assets/module_08/

# --- Trainer / private (answer keys, coaching, audits) ---
Instructor_Materials/
**/Instructor_Materials/
**/*_Instructor.md
**/*_Student.md
_private_validation/
**/_private_validation/
assets/REAL_VS_LAB_DATA.md
**/REAL_VS_LAB_DATA.md

# --- Authoring aids (not participant courseware) ---
master-prompt.md
course-content.md
*.docx
*.doc
*.pptx
*.ppt

# --- Secrets / credentials (never commit) ---
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials.json
credentials.csv
secrets/
**/secrets/
*accessKey*
*secret*access*
*.aws/
.aws/credentials

# --- Editor / OS / temp ---
~$*
*.tmp
*.temp
*.bak
*.swp
*~
Thumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/
*.code-workspace

# --- Local tooling / agent noise (if present under repo) ---
.cursor/
agent-transcripts/
**/terminals/
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# --- Archives that might accidentally include full repo dumps ---
*.zip
*.7z
*.rar
ADX_AWS_Training_Student/
**/ADX_AWS_Training_Student/
