# Azure Data Explorer (ADX) and AWS Integration

Day 1 courseware. Use your assigned login (`u01`–`u06`). Database name: `ADXTrainingDB_<your-login>`.

Open the lab VS Code URL from your access card. Course files are already in `~/adx-aws-training`. Follow `00_Day1_How_We_Work.md`, then Module 01.
